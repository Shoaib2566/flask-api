EOF'
If the test job fails, the build-docker job will not run because build-docker has needs: test in the GitHub Actions workflow.

This is important because Docker images should only be built after the code has passed automated tests. It prevents broken code from being packaged into a Docker image and saves CI resources by stopping the pipeline early when tests fail.
EOF